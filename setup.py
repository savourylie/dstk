from distutils.core import setup

setup(
  name = 'dstk',
  packages = ['dstk'], # this must be the same as the name above
  version = '0.1',
  description = 'A toolkit for data scientists.',
  author = 'Calvin Ku',
  author_email = 'calvin.j.ku@googlemail.com',
  url = 'https://github.com/savourylie/dstk.git', # use the URL to the github repo
  download_url = 'https://github.com/savourylie/dstk/archive/0.1.tar.gz', # I'll explain this in a second
  keywords = ['testing', 'logging', 'example'], # arbitrary keywords
  classifiers = [],
)